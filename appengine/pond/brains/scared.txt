/* Moves south-west when hit.  Does not fire. */
var d = damage();
while (true) {
  if (d != damage()) {
    drive(315, 100);
    var t = 0;
    for (var t = 0; t < 100; t++) {}
    d = damage();
    drive(0, 0);
  }
}
